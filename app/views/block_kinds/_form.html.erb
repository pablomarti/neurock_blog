<%= form_for(@block_kind) do |f| %>
  <% if @block_kind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@block_kind.errors.count, "error") %> prohibited this block_kind from being saved:</h2>

      <ul>
      <% @block_kind.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
