<h1>Listing block_kinds</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @block_kinds.each do |block_kind| %>
  <tr>
    <td><%= block_kind.title %></td>
    <td><%= block_kind.description %></td>
    <td><%= link_to 'Show', block_kind %></td>
    <td><%= link_to 'Edit', edit_block_kind_path(block_kind) %></td>
    <td><%= link_to 'Destroy', block_kind, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Block kind', new_block_kind_path %>
